<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= title %></title>
  <style>
    body.dark span {
      color: #f7fafc;
      background-color: black;
    }
  </style>
</head>
<body class="bg-white min-h-screen flex flex-col px-5">
  <%- include('../General/header') %>
  


  <!-- Chapter Title -->
  <div class="mt-6">

    <h1 class="text-4xl font-bold text-gray-900 mb-1 mt-3">
      <%= chapter.chaptername %>
    </h1>
    <p class="text-gray-700 mb-4 text-base mt-5">
      <%= chapter.description %>
    </p>
  </div>
  <hr class="my-6 border-t-2 border-gray-400 w-1/2">
  <!-- Pages Heading -->
  <h2 class="text-2xl font-bold underline text-gray-800 mt-5">Pages</h2>

  <!-- Pages List -->
  <% if (pages.length === 0) { %>
    <p class="text-gray-600 mt-2">No pages found for this chapter.</p>
  <% } else { %>
    <div class="flex flex-col gap-3 mt-4 w-full md:w-1/2">
      <% pages.forEach(page => { %>
        <a href="/student/page/<%= page.id %>" class="block border border-black px-4 py-3 my-2 bg-white hover:bg-gray-100 shadow-sm hover:shadow-md transition duration-200">
          <div class="flex justify-between items-center">
            <span class="font-medium"><%= page.title %></span>
            <% if (page.complete) { %>
              <span class="text-green-800 border border-green-500 px-4 bg-green-100 rounded font-semibold">Completed</span>
            <% } %>
          </div>
        </a>
      <% }) %>
    </div>
  <% } %>
</body>
</html>
