<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Educator | Course Enrollment Reports</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    footer {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background-color: #f3f4f6;
      padding: .35rem 0;
      text-align: center;
      box-shadow: 0 -1px 5px rgba(0, 0, 0, 0.1);
    }
</style>
<body class="bg-gray-50 text-gray-900 font-sans">
  <div class="mt-6">
    <nav class="text-l mb-2">
      <a href="/student" class="underline text-blue-600 ml-5">Dashboard</a> &gt;
      <span class="text-black-600">Reports</span>
    </nav>
  </div>

  <div class="p-6 max-w-5xl mx-auto">
    <h1 class="text-3xl font-bold mb-6">Course Enrollment Reports</h1>

    <% if (reportData.length === 0) { %>
      <div class="text-center text-gray-500 mt-12">
        <p>No courses available to display reports.</p>
      </div>
    <% } else { %>
      <table class="w-full table-auto border border-gray-300 shadow-md rounded-lg overflow-hidden">
        <thead class="bg-gray-100 text-left">
          <tr>
            <th class="px-4 py-2">#</th>
            <th class="px-4 py-2">Course Title</th>
            <th class="px-4 py-2">Total No Of Chapters</th>
            <th class="px-4 py-2">Total No Of Pages</th>
            <th class="px-4 py-2">Completed Percentage</th>
          </tr>
        </thead>
            <tbody>
            <% reportData.forEach((report, index) => { %>
                <tr class="border-t hover:bg-gray-50">
                <td class="px-4 py-2"><%= index + 1 %></td>
                <td class="px-4 py-2 font-medium">
                <a href="courses/<%= report.course.id %>/chapters" class="text-black-600 hover:text-blue-800">
                    <%= report.course.coursename %>
                </a>
                </td>
                <td class="px-4 py-2"><%= report.totalChapters %></td>
                <td class="px-4 py-2"><%= report.totalPages %></td>
                <td class="px-4 py-2">
                    <div class="flex items-center gap-2">
                    <div class="w-full bg-gray-200 rounded-full h-2.5">
                        <% if (typeof report.percentage !== "undefined") { %>
                        <div class="bg-green-500 h-2.5 rounded-full" style="width: <%= report.percentage %>%"></div>
                        <% } %>
                    </div>
                    <span class="text-sm text-gray-600 whitespace-nowrap"><%= report.percentage %>%</span>
                    </div>
                </td>
                </tr>
            <% }) %>
            </tbody>
      </table>
    <% } %>
  </div>

</body>
  <footer class="mt-auto p-4 flex justify-center items-center gap-4 text-sm text-gray-500">
      <button onclick="history.back()" class="bg-gray-600 text-white px-6 py-2 rounded hover:bg-gray-700 transition">
        Go Back
      </button>
  </footer>
</html>
