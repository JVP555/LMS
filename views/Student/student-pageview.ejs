<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= title %></title>
  <style>
    body { display: flex; flex-direction: column; height: 100vh; }
    main { flex: 1; overflow-y: auto; padding-bottom: 120px; }
    .fixed-buttons {
      position: fixed; bottom: 0px; left: 0; width: 100%;
      background-color: #ffffff; padding: .35rem 0;
      box-shadow: 0 -1px 5px rgba(0, 0, 0, 0.1);
      display: flex; justify-content: center; gap: 1rem;
    }
    header {
      position: sticky; top: 0;
      background-color: #ffffff; z-index: 10; padding: .5rem 0;
    }
  </style>
</head>
<body class="bg-gray-50 font-sans text-gray-800">
  <%- include('../General/header') %>
  <div class="mt-6">

  <header class="text-center mt-8 mb-4 px-4">
    <h1 class="text-4xl font-bold"><%= page.title %></h1>
  </header>

  <main class="px-6">
    <div class="max-w-3xl text-lg leading-relaxed text-center m-auto">
      <p><%= page.content %></p>
    </div>
  </main>

  <div class="fixed-buttons">
    <% if (prevPage) { %>
      <a href="/student/page/<%= prevPage.id %>" class="w-40">
        <button class="w-full bg-gray-300 hover:bg-gray-400 text-black font-medium px-4 py-2 rounded shadow h-full">
          Previous Page
        </button>
      </a>
    <% } else { %> <div class="w-40"></div> <% } %>

    <form action="/student/markCompleted/<%= page.id %>" method="POST" class="w-40 text-center">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <button type="submit"
        class="w-full whitespace-nowrap font-medium px-3 py-2 rounded shadow h-full text-white
        <%= page.completed ? 'bg-green-500 hover:bg-green-600' : 'bg-yellow-500 hover:bg-yellow-600' %>"
        aria-label="<%= page.completed ? 'Page already completed' : 'Mark this page as completed' %>">
        <%= page.completed ? 'Completed' : 'Mark as Completed' %>
      </button>
    </form>

    <% if (nextPage) { %>
      <a href="/student/page/<%= nextPage.id %>" class="w-40">
        <button class="w-full bg-blue-500 hover:bg-blue-600 text-white font-medium px-4 py-2 rounded shadow h-full">
          Next Page
        </button>
      </a>
    <% } else { %> <div class="w-40"></div> <% } %>
  </div>
</body>
</html>
