<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title><%= title %></title>
  <style>
    footer {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background-color: #f3f4f6;
      padding: .5rem 0;
      text-align: center;
      box-shadow: 0 -1px 5px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body class="bg-white min-h-screen flex flex-col px-5">

  <!-- Chapter Title -->
  <div class="mt-6">
    <nav class="text-l mb-2">
      <a href="/student" class="underline text-blue-600">Home</a> &gt;
      <a href="/student/courses/<%= chapter.courseId %>/chapters" class="underline text-blue-600">
        <%= chapter.coursename || "Course" %>
      </a> &gt;
      <span class="text-black-600"><%= chapter.chaptername %></span>
    </nav>

    <h1 class="text-4xl font-bold text-gray-900 mb-1 mt-3">
      <%= chapter.chaptername %>
    </h1>
    <p class="text-gray-700 mb-4 text-base mt-5">
      <%= chapter.description %>
    </p>
  </div>
  <hr class="my-6 border-t-2 border-gray-400 w-2/5">
  <!-- Pages Heading -->
  <h2 class="text-2xl font-bold underline text-gray-800 mt-5">Pages</h2>

  <!-- Pages List -->
  <% if (pages.length === 0) { %>
    <p class="text-gray-600 mt-2">No pages found for this chapter.</p>
  <% } else { %>
    <div class="flex flex-col gap-3 mt-4 w-full md:w-2/5">
      <% pages.forEach(page => { %>
        <a href="/student/page/<%= page.id %>" class="block font-medium border border-black px-4 py-3 my-2 bg-white hover:bg-gray-100 shadow-sm hover:shadow-md transition duration-200">
          <%= page.title %>
        </a>
      <% }) %>
    </div>
  <% } %>

  <!-- Footer -->
  <footer class="mt-auto p-4 flex justify-center items-center gap-4 text-sm text-gray-500 border-t">
    <button onclick="window.location.href='/student/courses/<%= chapter.courseId %>/chapters'" class="bg-gray-700 text-white px-5 py-2 rounded hover:bg-gray-800 transition">
      Go Back
    </button>
    <a href="/student" class="underline hover:text-gray-700 transition">
      Back to Dashboard
    </a>
  </footer>

</body>
</html>
