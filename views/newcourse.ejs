<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create New Course</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white p-10 font-sans">
  <!-- Breadcrumb -->
  <nav class="text-sm mb-4">
    <a href="/Educator" class="text-blue-600 underline">Home</a>
    <span class="mx-1">&gt;</span>
    <span>Create New Course</span>
  </nav>

  <!-- Heading -->
  <h1 class="text-4xl font-bold mb-6">Create New Course</h1>

  <!-- Form -->
  <form action="/courses" method="POST" class="space-y-4 max-w-md">
    <label for="courseName" class="block text-gray-600 text-sm font-semibold">
      What is the name of this course?
    </label>
    <input 
      type="text" 
      name="courseName" 
      id="courseName" 
      class="w-full border border-black rounded px-3 py-2 focus:outline-none" 
      value="JavaScript"
    />

    <button 
      type="submit" 
      class="px-4 py-2 border border-black text-black font-semibold hover:bg-gray-100 transition rounded"
    >
      Submit
    </button>
  </form>
</body>
</html>
