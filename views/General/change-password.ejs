<!DOCTYPE html>
<html lang="en">
<head>
  <title>Change Password</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-100 min-h-screen flex flex-col items-center justify-center font-sans text-slate-800">

  <%- include('header') %>
  <%- include('notifications', { messages: messages }) %>

  <div class="bg-white shadow-lg rounded-2xl p-8 w-full max-w-md border border-slate-200">
    <h1 class="text-3xl font-bold text-center mb-6 text-slate-700">🔐 Change Password</h1>

    <form action="/changepassword/<%= user.id %>" method="POST" class="space-y-5">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <div>
        <label class="block text-slate-700 font-medium mb-1">Current Password</label>
        <input
          type="password"
          name="currentPassword"
          class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
          placeholder="Enter current password"
          required />
      </div>

      <div>
        <label class="block text-slate-700 font-medium mb-1">New Password</label>
        <input
          type="password"
          name="newPassword"
          class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
          placeholder="Enter new password"
          required />
      </div>

      <button
        type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg transition shadow">
        Update Password
      </button>
    </form>

    <div class="mt-4 text-center">
      <a
        href="/<%= user.role === 'educator' ? 'Educator' : 'Student' %>"
        class="text-sm text-blue-600 hover:underline hover:text-blue-700 transition">
        ⬅ Back to Dashboard
      </a>
    </div>
  </div>

</body>
</html>
