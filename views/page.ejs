<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <title><%= title %></title>
</head>
<body class="min-h-screen flex flex-col bg-gray-50 font-sans text-gray-800">

  <div class="p-4">
    </div>

  <header class="text-center mt-8 mb-4 px-4">
    <h1 class="text-4xl font-bold"><%= page.title %></h1>
  </header>

  <main class="flex-1 flex items-center justify-center px-6">
    <div class="max-w-3xl text-lg leading-relaxed text-center">
      <p><%= page.content %></p>
    </div>
  </main>

  <footer class="flex justify-between items-center px-6 pb-6 gap-2">
    <% if (prevPage) { %>
      <a href="/page/<%= prevPage.id %>" class="w-40">
        <button class="w-full bg-gray-300 hover:bg-gray-400 text-black font-medium px-4 py-2 rounded shadow">
          Previous Page
        </button>
      </a>
    <% } else { %>
      <div class="w-40"></div>
    <% } %>

    <form action="/markCompleted/<%= page.id %>" method="POST" class="w-40 text-center">
      <button type="submit" class="w-full <%= page.completed ? 'bg-green-500 hover:bg-green-600' : 'bg-blue-500 hover:bg-blue-600' %> text-white font-medium px-4 py-2 rounded shadow" aria-label="<%= page.completed ? 'Page already completed' : 'Mark this page as completed' %>">
        <%= page.completed ? 'Completed' : 'Mark as Completed' %>
      </button>
    </form>

    <% if (nextPage) { %>
      <a href="/page/<%= nextPage.id %>" class="w-40">
        <button class="w-full bg-blue-500 hover:bg-blue-600 text-white font-medium px-4 py-2 rounded shadow">
          Next Page
        </button>
      </a>
    <% } else { %>
      <div class="w-40"></div>
    <% } %>
  </footer>

  <div class="text-center pb-6">
    <a href="/Educator" class="text-blue-600 hover:underline">Back to Chapter</a>
  </div>

</body>
</html>